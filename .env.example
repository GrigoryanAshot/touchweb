# Copy this file to .env and fill in (never commit .env).

# Inbox FormSubmit delivers to (same as your FormSubmit activation email).
FORMSUBMIT_EMAIL=touchwebagency@gmail.com

# Optional: port for local server (default 3000)
# PORT=3000
